#!/usr/bin/env bash
set -euxo pipefail

export PLAYWRIGHT_BROWSERS_PATH="$PWD/.pw-browsers"
mkdir -p "$PLAYWRIGHT_BROWSERS_PATH"

python -m playwright install chromium

echo "Installed browsers at: $PLAYWRIGHT_BROWSERS_PATH"
ls -al "$PLAYWRIGHT_BROWSERS_PATH" || true
